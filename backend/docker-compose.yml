services:
  postgres:
    image: postgres:16-alpine
    container_name: quickdoctor-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: quickdoctor
    ports:
      - "5432:5432"
    volumes:
      - quickdoctor_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d quickdoctor"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  quickdoctor_pgdata:
